<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <meta charset="utf-8">
  <title>to Do List!</title>

  <link rel="stylesheet" href="CSS/styles.css">
</head>

<body>
  <div class="box" id="heading">
    <h1><%=keyDay%></h1>
  </div>
  <div class="box">
    <% keyItem.forEach(function(items){ %>
      <form  action="/delete" method="post">
    <div class="item">
      <input type="checkbox" name="checkboxed" value="<%=items._id %>" onChange="this.form.submit()">
      <p><%= items.name %></p>
    </div>
    <input type="hidden" name="hiddenValue" value="<%=keyDay%>"></input>
      </form>
    <% }) %>

    <form class="item" action="/" method="post">
      <input type="text" name="item" placeholder="next item">
      <button type="submit" name="buttonName" value="<%=keyDay%>">🡲</button>
    </form>

  </div>



</body>

</html>
